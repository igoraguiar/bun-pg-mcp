{
  "name": "pg-mcp",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "version": "1.1.0-next",
  "license": "MIT",
  "main": "dist/index.js",
  "bin": {
    "pg-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build --outdir dist --target bun ./src/index.ts",
    "compile": "bun build --compile --outfile=out/pg-mcp ./src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.25.76"
  }
}
